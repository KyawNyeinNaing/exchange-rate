'use client';

import { Button as MTButton } from '@material-tailwind/react';

const Button: React.FC = () => {
  return <MTButton className="bg-gray-800">Swap Currency</MTButton>;
};

export default Button;
